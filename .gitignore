dump_production.sql
.env.production
.claude/
node_modules/
apps/api/data/dgi-pdfs/
.env
.env.*
!.env.example

# Build artifacts (régénérables)
apps/desktop/dist/
apps/api/dist/web/
# Sortie du build front : Cloudflare Pages la reconstruit à chaque déploiement
# (npm --prefix apps/web run build → apps/web/dist).
apps/web/dist/
*.bak
*.bak_avant_*

# Backups & données runtime
backups/
exports/
data/
storage/
