import React from "react";
import LandingV4 from "./LandingV4";

/**
 * Landing — UI only.
 * - No auth logic.
 * - No API calls.
 * - No business rules.
 *
 * Current active landing: V4 (HTML-based).
 */
export default function LandingPage() {
  return <LandingV4 />;
}
