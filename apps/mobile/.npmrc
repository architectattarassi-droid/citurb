# =====================================================
# .npmrc — apps/mobile
# =====================================================
# Désactive le hoisting NPM pour Capacitor (le sync nécessite que les plugins
# soient dans apps/mobile/node_modules/ et pas hoist au root du monorepo).

node-linker=hoisted
package-lock=true
auto-install-peers=true
strict-peer-dependencies=false
