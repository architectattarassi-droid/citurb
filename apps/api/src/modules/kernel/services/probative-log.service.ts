/**
 * Placeholder.
 * In the full implementation this will:
 * - append-only ProbativeLog with hash chain (batching supported)
 */
export class ProbativeLogService {
  async append(_payload: unknown) {
    return { ok: true };
  }
}
